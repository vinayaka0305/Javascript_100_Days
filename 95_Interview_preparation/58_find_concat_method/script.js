const arr = [1,2,3,4,5];

const newArr = arr.find((num)=>num>4);

console.log(newArr);

// find: get the first number greater than 4



const arr1 = [1,2,3];
const arr2 = [4,5,6];

let combined = arr1.concat(arr2);
console.log(combined);

// The concat() method is used to merge two or more arrays into a new array without changing the original arrays.
